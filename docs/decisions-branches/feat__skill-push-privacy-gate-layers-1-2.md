## 2026-06-03 15:19 - feat(skill push): privacy gate layers 1+2 (frontmatter + dir convention) (§8.2 first slice)

**Reasoning:** Master plan §8.2 first slice — belt-and-braces layers 1 (frontmatter markers `private: true` / `do-not-promote: true`) and 2 (`.claude/skills-private/` directory convention). Layer 1 fires after `CheckFrontmatter` on a well-formed body and rejects before any clone work runs. Layer 2 fires BEFORE the SKILL.md read (a path stat on `.claude/skills-private/<name>/SKILL.md`) so a skills-private skill is rejected without touching its bytes — directory placement is the primary signal. Both layers exit non-zero through `skill.ErrPrivateSkill` (a new sentinel) joined with `clierr.ErrSilent` so the cli layer translates to silent-exit cleanly. The `clierr.ErrSilent` sentinel was promoted from `internal/cli/install_hook.go` to a new tiny `internal/clierr/` package because the skill package can't import cli (would cycle) but the skill package now needs to wrap the silent-exit sentinel at error-construction time. `internal/cli/install_hook.go` keeps the same `ErrSilent` name as an alias of `clierr.ErrSilent` — all existing cli tests and call sites continue working unchanged because `errors.Is` identity is preserved. The two-layer precedence (dir wins over explicit `private: false`) is locked by `TestPush_SkillsPrivateDir_BeatsExplicitFalseOverride` so a future contributor can't accidentally defer to the frontmatter. No `--force` flag — these are guard rails, not toggles; users move the skill out of skills-private or remove the marker if the push is intentional. Frontmatter parser accepts `true`/`yes`/`on` (case-insensitive YAML 1.1 booleans) but NOT numeric `1` (avoids false-positive on stray version strings).

**Alternatives considered:** Custom error type with `Unwrap()` returning both sentinels — rejected because Go's `errors.Is` walks one Unwrap chain at a time and chained `%w` doesn't broadcast `Is` across both wrapped errors in all stdlib versions; `errors.Join` is the canonical multi-sentinel pattern. Define `ErrSilent` in `internal/skill/` — rejected because `cli/install_hook.go` already owns the cobra-side semantics and moving it there would force the skill package to own a CLI concept it doesn't otherwise care about. Add `--force` flag to bypass the gate — rejected per master plan §8.2: belt-and-braces means the gates ARE the contract; users with legitimate need move the skill or clear the marker (an explicit, auditable action). Inline the dir-convention check via `filepath.Join` in push.go without a new `SkillsPrivateDir` helper — rejected because future logmind tooling (`skill list --private`, content-scanner wave-2) will pivot on the same canonical path; a helper centralises it.

**Implications:**
- New sentinel `skill.ErrPrivateSkill` in `internal/skill/push.go`; constructed via `newPrivateSkillError()` helper that `errors.Join`s a formatted user message, `ErrPrivateSkill`, and `clierr.ErrSilent` so both `errors.Is` checks succeed.
- New tiny package `internal/clierr/` holds `ErrSilent`. `internal/cli/install_hook.go`'s `var ErrSilent = clierr.ErrSilent` keeps the existing surface intact — every test that grabbed `cli.ErrSilent` still passes.
- New helper `skill.SkillsPrivateDir(repoRoot string)` alongside `SkillsDir`; centralises the `.claude/skills-private/` path for future tooling.
- New regex `privateFieldRE` in `internal/skill/push.go` matches `(?im)^\s*(private|do-not-promote)\s*:\s*(true|yes|on)\s*$` — anchored multi-line, case-insensitive on keyword + value.
- Tests: 5 new functional tests (`TestPush_PrivateFrontmatter_RejectedBeforeClone`, `TestPush_DoNotPromoteFrontmatter_Rejected`, `TestPush_SkillsPrivateDir_RejectedByConvention`, `TestPush_SkillsPrivateDir_BeatsExplicitFalseOverride`, `TestPush_HappyPath_NoPrivacyMarkers_Unaffected`) + 1 parser unit test with 12 sub-cases (`TestScanPrivateFrontmatterField_DetectsAllAcceptedSpellings`) + 2 cli-layer tests (`TestSkillPush_PrivateFrontmatterReturnsErrSilent`, `TestSkillPush_SkillsPrivateDirReturnsErrSilent`) — all pass under `go test ./internal/skill/... ./internal/cli/...`.
- Help text on `logmind skill push --help` gains a "Privacy gate (§8.2, layers 1+2)" section documenting both rejection paths.
- README.md gains a Quick Start example for `logmind skill push` + a dedicated "`logmind skill push` privacy gate" subsection.
- Wave-2 layers (3 = content scanner; 4 = repo-visibility check) are NOT shipped here per the master plan first-slice framing; promotion detector is queued as a separate larger dispatch on agent-skills or skill-telemetry.

---
