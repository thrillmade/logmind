# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
*.egg
MANIFEST

# Virtual environments
venv/
env/
ENV/
.venv/
.env/

# Testing
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
coverage.xml
*.cover
.hypothesis/

# Type checking
.mypy_cache/
.ruff_cache/
.pytype/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# logmind
.logmind/cache/
.logmind/.lock

# skills.sh tooling (project-scoped lockfile, not for main repo)
skills-lock.json
.agents/

# Agent worktrees — per-session scratch trees created by Claude Code's
# Agent(isolation: "worktree"). Local-only; never commit.
.claude/worktrees/

# Intentionally untracked: live in separate repos (Homebrew tap lives
# at thrillmade/homebrew-tap, not in this repo).
homebrew-logmind/
homebrew-tap/

# Go build artefacts — source lives in cmd/ and internal/.
bin/
*.test
*.out
.playwright-mcp/
