name: publish

# Triggered on tag push (e.g. v0.1.0). Builds the sdist + wheel and pushes
# to PyPI via OIDC (configure logmind as a "Trusted Publisher" on PyPI;
# no long-lived API token required). Then creates a GitHub Release with
# the artifacts and the matching CHANGELOG section.

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write    # for the GitHub Release
  id-token: write    # for PyPI OIDC

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Bundle CHANGELOG into package
        # CHANGELOG.md is canonical at repo root (GitHub auto-renders it,
        # publish.yml's release-notes step reads from there) but must be
        # under src/logmind/ for setuptools package-data to bundle it
        # into the wheel. Copy at build time so the file stays in one
        # place in source control; editable installs fall back to the
        # repo-root copy via logmind.core.changelog._changelog_path().
        run: cp CHANGELOG.md src/logmind/CHANGELOG.md

      - name: Build
        run: |
          python -m pip install --upgrade pip build
          python -m build

      - uses: actions/upload-artifact@v7
        with:
          name: dist
          path: dist/

  pypi:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/logmind
    steps:
      - uses: actions/download-artifact@v8
        with:
          name: dist
          path: dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1

  release:
    needs: pypi
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/download-artifact@v8
        with:
          name: dist
          path: dist/

      - name: Extract changelog section
        id: changelog
        shell: bash
        run: |
          tag="${GITHUB_REF_NAME#v}"
          # Pull the [tag] section out of CHANGELOG.md (best-effort; falls
          # back to the [Unreleased] section if the tag heading isn't there yet).
          awk -v tag="$tag" '
            $0 ~ "^## \\[" tag "\\]" { in_section=1; next }
            in_section && /^## \[/ { exit }
            in_section { print }
          ' CHANGELOG.md > release-notes.md
          if [ ! -s release-notes.md ]; then
            awk '
              /^## \[Unreleased\]/ { in_section=1; next }
              in_section && /^## \[/ { exit }
              in_section { print }
            ' CHANGELOG.md > release-notes.md
          fi

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          body_path: release-notes.md
          files: dist/*
          generate_release_notes: false
