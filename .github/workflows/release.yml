name: release

on:
  push:
    tags:
      - "v[1-9]*"
  workflow_dispatch:
    inputs:
      dry_run:
        description: "snapshot mode"
        required: false
        default: "true"

permissions:
  contents: write
  packages: write

jobs:
  release:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: actions/setup-go@v5
        with:
          go-version: "1.22"
          cache: true
      - name: Import Apple codesign cert
        uses: Apple-Actions/import-codesign-certs@v3
        with:
          p12-file-base64: placeholder-base64
          p12-password: placeholder-password
      - name: hello
        run: echo "release triggered for tag=${{ github.ref_name }} event=${{ github.event_name }}"
